# AI Assistant Risk Evaluation Workbench

Compare open-source and frontier assistants through the lens of AI risk.

Use the settings panel to switch models, tune generation, and decide whether unsafe inputs should be blocked before model execution. Each response includes a side-panel trace with latency, guardrail status, and safety categories.

Use the action buttons to reset memory, run a small smoke eval, or generate the PDF evaluation report after results exist.
